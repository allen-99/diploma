{% include "header.html.jinja2" %}
<div class="hero bg-base-200 h-full">
    <div class="hero-content flex-col lg:flex-row-reverse">
        <div class="text-center lg:text-left">
            <h1 class="text-5xl font-bold">Вход</h1>
            <p class="py-6">Войдите в аккаунт для того, чтобы пользоваться всеми функциями приложения</p>
        </div>
        <form method="post" class="card flex-shrink-0 w-full max-w-sm shadow-2xl bg-base-100">
            {% csrf_token %}
            <div class="card-body">
                {% if form.errors %}
                    ошибка емае
                {% endif %}
                <label class="label" for="username">
                    <span class="label-text">Логин</span>
                    <input type="text" name="username" placeholder="Введите логин" class="input input-bordered"/>
                </label>
                <label class="label" for="password">
                    <span class="label-text">Пароль</span>
                    <input type="password" name="password" placeholder="Введите пароль" class="input input-bordered"/>
                </label>
                <label class="label">
                    <a href="{% url 'signup' %}" class="label-text-alt link link-hover">Нет аккаунта?
                        Зарегестрируйтесь!</a>
                </label>
                <input type="hidden" name="next" value="{{ next }}"/>
                <div class="form-control mt-6">
                    <button type="submit" class="btn btn-primary">Войти!</button>
                </div>
            </div>
        </form>
    </div>
</div>